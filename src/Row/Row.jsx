import React, { useContext } from 'react'
import { Context } from '../Context'

export const Row = ({quizz, onClickOpen}) => {
    const handleClickOpen = (id) => {
        onClickOpen(id)
    }
    return (
        <>
            
        </>
    )
}
